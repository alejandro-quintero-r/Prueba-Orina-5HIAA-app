const DB_USER = "admin";
const DB_PASSWORD = "admin123456"; 
const DB_HOST = "cluster0.wv8x2ch.mongodb.net";

const IP_SERVER = "localhost"

module.exports = {
    DB_USER, 
    DB_PASSWORD,
    DB_HOST, 
    IP_SERVER
}